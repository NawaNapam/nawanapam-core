<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <!-- Keep brand-green visible while WebView loads — prevents white flash -->
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- Full-screen brand green — matches the logo badge colour -->
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <!-- The logo itself (icon-safe, rounded-square badge) -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash</item>
        <!-- Android 12+: make the icon background also brand-green so there's
             no white circle visible behind the logo badge shape -->
        <item name="windowSplashScreenIconBackgroundColor">@color/splashIconBackground</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>